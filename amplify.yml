version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd portfolio
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: portfolio/dist
    files:
      - '**/*'
  cache:
    paths:
      - portfolio/node_modules/**/*